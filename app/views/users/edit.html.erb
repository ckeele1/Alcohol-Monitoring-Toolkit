<h1>Editing user</h1>

<%= form_for(@user) do |f| %>
	<%= render :partial => 'form', :locals => { :f => f } %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>


<%= link_to 'Back', users_path %>
